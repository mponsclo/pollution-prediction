# Minimal dependencies for the Streamlit dashboard when hosted on
# Streamlit Community Cloud. The full requirements.txt pulls in torch,
# dbt, mlflow, fastapi, etc. — none of which the dashboard needs — and
# torch alone busts the Cloud container disk limit.
#
# Point Streamlit Cloud's "Advanced settings → Python requirements file"
# at this file to deploy in parquet mode.

pandas==2.3.2
numpy==2.3.2
pyarrow>=15.0.0
streamlit>=1.28.0
plotly>=5.15.0
folium==0.20.0
streamlit-folium>=0.13.0

# Only needed when running with DATA_BACKEND=bigquery.
# Uncomment + configure GCP creds in Streamlit secrets to re-enable.
# google-cloud-bigquery>=3.0.0
# db-dtypes
